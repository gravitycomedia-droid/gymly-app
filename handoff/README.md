# Gymloop frontend handoff

- **`INTEGRATION.md`** — every page, the route map, prop contracts, design tokens, and the step-by-step porting recipe.
- **`GYMLOOP-FULL-SOURCE.md`** — all code of the frontend in one file.
- **`src/`** — the raw source files.

Start with `INTEGRATION.md` §4 (Porting recipe). The screens live in
`GymloopApp.dc.html`; everything else is prototype plumbing.
